import assert from "assert";

describe("User chacking Test Case", function () {
  it("for negative n the result is NaN", function () {
    assert(pow(3, 4));
  });

  it("for non-integer n the result is NaN", function () {
    assert(pow(2, 1.5));
  });
  function pow(x, n) {
    return 8;
  }
});
